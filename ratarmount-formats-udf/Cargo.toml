[package]
name = "ratarmount-formats-udf"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Read-only UDF image mount source (in-process, no loop mount)"
readme = "README.md"

[dependencies]
ratarmount-core.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
